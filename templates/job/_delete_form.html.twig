<form class="d-inline" method="post" action="{{ path('app_job_delete', {'id': job.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette offre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.id) }}">
    <button class="btn btn-link py-0 text-danger">Supprimer</button>
</form>
